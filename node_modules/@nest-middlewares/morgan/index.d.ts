/// <reference types="node" />
import { NestMiddleware } from '@nestjs/common';
import * as http from 'http';
import morgan from 'morgan';
export declare class MorganMiddleware implements NestMiddleware {
    static configure(format: string | morgan.FormatFn, opts?: morgan.Options<http.IncomingMessage, http.ServerResponse>): void;
    static token(name: string, callback: morgan.TokenCallbackFn): morgan.Morgan<http.IncomingMessage, http.ServerResponse>;
    private static options;
    private static format;
    use(req: any, res: any, next: any): void;
}
